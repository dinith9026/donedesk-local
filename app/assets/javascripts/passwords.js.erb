$(function() {
  $("#password_reset_password, #registration_password").passwordRequirements({
    minLength: 8
  });

  $("#password_reset_password, #registration_password").pwstrength({
    meterId: '#password-strength-meter',
    textId: '#password-strength-text',
    suggestionsId: '#password-strength-suggestions',
  });
});
