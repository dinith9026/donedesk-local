Hello <%= @employee_record.first_name %>

You've been assigned <%= pluralize(@new_assignments.count, 'new course') %>:

<% @new_assignments.each do |assignment| %>
  <%= assignment.course_title %> - <%= assignment.course_description %>
<% end %>

Time's ticking! Login to take your <%= 'course'.pluralize(@new_assignments.count) %> now:
<%= assignments_url %>
